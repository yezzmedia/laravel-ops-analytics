# Ops Analytics Checklist

- confirm descriptor capability contracts still match the real package surface
- confirm install steps, doctor checks, features, and ops modules stay aligned with pages and runtime services
- confirm request capture stays sanitized and config-driven
- confirm built-in tracker bootstrap remains explicit and testable
- confirm traffic pages still degrade safely when storage is unavailable
- confirm `composer test:ops-analytics` passes from `1-dev-test`
